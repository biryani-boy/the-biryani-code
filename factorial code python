def factorial(x):
    if (x==0 or x==1):
        return 1;
    else:
        return (x*fact(x-1));

num=int(input("enter number for factorial"))
print("the factorial of "+str(num)+" is:")
print(factorial(num))
